function renderContactModal(id) {
  const application=applicationById(id);
  return overlayFrame(`<div class="modal-head"><div><span class="eyebrow">${escapeHtml(application.guestName||application.phone)}</span><h2>Результат контакта</h2><p>Результат будет записан в общую timeline. Статус меняется отдельно.</p></div><button class="icon-button" data-action="close-overlay">${icon('close')}</button></div><form data-form="contact" data-application-id="${id}"><div class="outcome-grid">${Object.entries(CONTACT_OUTCOMES).map(([key,label])=>`<label><input type="radio" name="outcome" value="${key}" ${key==='reached'?'checked':''}/><span>${icon(key==='no_answer'?'phone':key==='callback'?'clock':key==='declined'?'close':'check',18)}${label}</span></label>`).join('')}</div><label class="field"><span>Комментарий</span><textarea name="note" rows="3" placeholder="Кратко зафиксируйте договорённость"></textarea></label><label class="field callback-field"><span>Перезвонить</span><input type="datetime-local" name="callbackAt"/></label><div class="modal-actions"><button type="button" class="button secondary" data-action="close-overlay">Отмена</button><button type="submit" class="button primary">Сохранить результат</button></div></form>`,{label:'Результат контакта'});
}

function renderPaymentModal(id) {
  const application=applicationById(id); const suggested=Math.max(0,(application.status==='awaiting_prepayment'?application.requiredPrepayment:application.total)-application.paid);
  return overlayFrame(`<div class="modal-head"><div><span class="eyebrow">Проверка поступления</span><h2>Подтвердить платёж</h2><p>Чек без ручной проверки не меняет полученную сумму.</p></div><button class="icon-button" data-action="close-overlay">${icon('close')}</button></div><div class="payment-context"><span>Ожидается</span><strong>${formatMoney(suggested)}</strong><small>Остаток по проживанию: ${formatMoney(Math.max(0,application.total-application.paid))}</small></div><form data-form="payment" data-application-id="${id}" class="form-grid"><label><span>Фактически получено</span><input name="amount" type="number" min="1" step="100" value="${suggested||10000}" required/></label><label><span>Назначение</span><select name="kind"><option value="rent">Аренда</option><option value="deposit">Депозит</option></select></label><label><span>Способ</span><select name="method"><option>Kaspi</option><option>Перевод по номеру Kaspi</option><option>Банковский перевод</option><option>Наличные</option><option>Другое</option></select></label><label><span>Дата и время</span><input type="datetime-local" name="receivedAt" value="${new Date(Date.now()-new Date().getTimezoneOffset()*60000).toISOString().slice(0,16)}"/></label><label class="span-2 upload-field"><span>${icon('upload',20)}Прикрепить чек <em>опционально</em></span><input type="file" name="receipt" accept="image/*,.pdf"/><small>В демо файл не отправляется на сервер.</small></label><label class="span-2"><span>Комментарий</span><input name="note" placeholder="Например: проверено в Kaspi Pay"/></label><div class="modal-actions span-2"><button type="button" class="button secondary" data-action="close-overlay">Отмена</button><button type="submit" class="button primary">Подтвердить поступление</button></div></form>`,{label:'Подтверждение платежа'});
}

function renderNoteModal(id) {
  const application=applicationById(id);
  return overlayFrame(`<div class="modal-head"><div><span class="eyebrow">Важная заметка</span><h2>Закрепить наверху</h2><p>Заметку увидит любой менеджер, открывший заявку.</p></div><button class="icon-button" data-action="close-overlay">${icon('close')}</button></div><form data-form="note" data-application-id="${id}"><label class="field"><span>Текст</span><textarea name="note" rows="4" maxlength="280" placeholder="Например: клиент прилетает поздно">${escapeHtml(application.pinnedNote||'')}</textarea></label><div class="modal-actions"><button type="button" class="button tertiary" data-action="clear-note" data-application-id="${id}">Удалить</button><button type="submit" class="button primary">Сохранить</button></div></form>`,{label:'Важная заметка'});
}

function renderStatusModal(id) {
  const application=applicationById(id);
  return overlayFrame(`<div class="modal-head"><div><span class="eyebrow">Текущий статус: ${statusMeta(application.status).label}</span><h2>Изменить статус</h2><p>Hard-статусы проходят серверную проверку конфликта.</p></div><button class="icon-button" data-action="close-overlay">${icon('close')}</button></div><div class="status-choice-list">${statusOrder.filter(key=>key!==application.status).map(key=>`<button data-action="choose-status" data-status="${key}" data-application-id="${id}"><span class="status-color status-${STATUS[key].tone}"></span><span><strong>${STATUS[key].label}</strong><small>${STATUS[key].hard?'Гарантированная занятость':'Не создаёт hard-блок'}</small></span>${icon('chevronRight',17)}</button>`).join('')}</div>`,{label:'Изменение статуса'});
}

function renderCompleteModal(id) {
  const application=applicationById(id);
  return overlayFrame(`<div class="modal-head"><div><span class="eyebrow">После выезда</span><h2>Завершить бронь</h2><p>Auto-complete отсутствует: менеджер подтверждает коммерческое закрытие вручную.</p></div><button class="icon-button" data-action="close-overlay">${icon('close')}</button></div><form data-form="complete" data-application-id="${id}" class="completion-form"><label class="check-row"><input type="checkbox" name="paid" ${application.paid>=application.total?'checked':''} required/><span><strong>Полная оплата подтверждена</strong><small>${formatMoney(application.paid)} из ${formatMoney(application.total)}</small></span></label><label class="check-row"><input type="checkbox" name="noRefund" required/><span><strong>Возврат не ожидается</strong><small>После завершения обычный возврат не планируется</small></span></label><label class="field"><span>Депозит</span><select name="depositOutcome"><option>Не применялся</option><option>Возвращён</option><option>Удержан частично</option><option>Удержан полностью</option></select></label><label class="field"><span>Комментарий</span><textarea name="note" rows="3" placeholder="Необязательно"></textarea></label><div class="modal-actions"><button type="button" class="button secondary" data-action="close-overlay">Отмена</button><button type="submit" class="button primary">Завершить бронь</button></div></form>`,{label:'Завершение брони'});
}

